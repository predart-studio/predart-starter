# Project Context References

Store the visual grounding assets for the project context pack here.

Suggested subfolders:
- `visual/` — screenshots from website references
- `motion/` — keyframes / motion snapshots / extracted video frames
- `pdf/` — rendered pages from brochures / brand books / decks

The markdown files in `docs/project-context/` should refer to these assets explicitly when they matter.
